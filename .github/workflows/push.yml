name: tests

on:
  push:
    branches:
      - "*"
  pull_request:

jobs:
  tests:
    name: py${{ matrix.python-version }}-${{ matrix.platform.architecture }}, ${{ matrix.platform.os }}
    runs-on: ${{ matrix.platform.os }}
    strategy:
      matrix:
        python-version: [3.5, 3.6, 3.7, 3.8, 3.9]
        # rust-toolchain: [stable, beta, nightly]
        rust-toolchain: [stable]
        platform: [
          #{ os: "ubuntu-latest",  architecture: "x64", rust-target: "x86_64-unknown-linux-gnu" },
          #{ os: "macOS-latest",   architecture: "x64", rust-target: "x86_64-apple-darwin" },
          { os: "windows-latest", architecture: "x64", rust-target: "x86_64-pc-windows-msvc" },
          # { os: "windows-latest", architecture: "x86", rust-target: "i686-pc-windows-msvc" },
        ]
      fail-fast: false
    env:
      CARGO_BUILD_TARGET: ${{ matrix.platform.rust-target }}
    steps:
      - uses: actions/checkout@v2
      - uses: actions/cache@v2
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-${{ matrix.platform.architecture }}-${{ hashFiles('**/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.platform.architecture }}-
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
          architecture: ${{ matrix.platform.architecture }}
      - run: python tests/python_info.py
      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: ${{ matrix.rust-toolchain }}
          target: ${{ matrix.platform.rust-target }}
          default: true
          profile: minimal
      - name: Install llvm for windows
        run: choco install llvm -y
        if: runner.os == 'Windows' && steps.choco-cache.outputs.cache-hit != 'true'
      - name: Install pytest
        run: pip install pytest
      - name: Run build.py
        run: python -u tests/build.py
      - name: Run pytest
        run: python -u -m pytest --verbose
